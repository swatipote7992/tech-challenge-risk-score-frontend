{
  "name": "risk-rule-challenge",
  "version": "0.1.0",
  "private": true,
  "dependencies": {},
  "devDependencies": {
    "ts-jest": "29.1.0",
    "ts-node": "10.9.1",
    "typescript": "^5.0.4"
  },
  "scripts": {
    "start": "node scripts/start.js",
    "build": "tsc",
    "test": "node scripts/test.js --env=jsdom",
    "zip": "zip -r --exclude=*.git* --exclude=*node_modules* --exclude=*idea* tech-challenge-risk-score-frontend.zip ."
  }
}